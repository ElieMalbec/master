//

/*Thoughts
We can try bit swap them bit by bit but it's would be quite long
*/

#include <iostream>
#include <bitset>

using std::cout;
using std::endl;
using std::bitset;


int main(void){
    
    int n = 1775;

    //cout << bitset<32>(n) << endl;
    //cout << flip(n) << endl;
    return 0;
}